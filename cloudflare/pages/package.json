{
  "name": "laptopshop-pages",
  "version": "1.0.0",
  "description": "Laptop Shop Frontend on Cloudflare Pages",
  "main": "index.html",
  "scripts": {
    "dev": "npm run serve",
    "serve": "npx serve . -p 8080",
    "build": "node build.js",
    "copy-assets": "node build.js",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "deploy:prod": "npm run build && npx wrangler pages deploy dist --env production",
    "preview": "npm run build && npx wrangler pages dev dist",
    "clean": "rm -rf dist",
    "postinstall": "npm install -g wrangler@latest"
  },
  "keywords": ["laptop", "ecommerce", "cloudflare", "pages"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.1"
  }
}